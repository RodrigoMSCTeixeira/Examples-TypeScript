// constructor Retorna a função que criou o protótipo Number do JavaScript

// Number.EPSILON Retorna a diferença entre 1 e o menor número maior que 1

// Number.isFinite( valor ) Verifica se um valor é um número finito

// Number.isInteger( valor ) Verifica se um valor é um número inteiro

// Number.isNaN( valor )	Verifica se um valor é Number.NaN

// Number.isSafeInteger( valor ) Verifica se um valor é um número inteiro seguro

// Number.MAX_SAFE_INTEGER Retorna o número inteiro seguro máximo em JavaScript.

// Number.MIN_SAFE_INTEGER	Retorna o mínimo inteiro seguro em JavaScript

// Number.MAX_VALUE Retorna o maior número possível em JavaScript

// Number.MIN_VALUE Retorna o menor número possível em JavaScript

// Number.NaN Representa um valor "Não é um número"

// Number.NEGATIVE_INFINITY Representa infinito negativo (retornado em estouro)

// Number.POSITIVE_INFINITY Representa infinito (retornado em estouro)

// parseFloat( valor )	Analisa uma string e retorna um número

// parseInt( valor ) Analisa uma string e retorna um número inteiro

// prototype Permite adicionar propriedades e métodos a um objeto

// toExponential( número? )	Converte um número em uma notação exponencial

// toFixed( dígitos? ) Formata um número com x números de dígitos após o ponto decimal

// toLocaleString( locales?, options? )	Converte um número em uma string, com base nas configurações de localidade

// toPrecision( dígitos? ) Formata um número para x comprimento

// toString( raiz ) Converte um número em uma string

// valueOf() Retorna o valor primitivo de um número

let pattern = /w3schools/i;

console.log(typeof pattern);
