// charAt( índice? ) Retorna o caractere em um índice especificado (posição)

// charCodeAt( índice? )	Retorna o Unicode do caractere em um índice especificado

// concat( string1, string2, ..., stringX )	Retorna duas ou mais strings unidas

// constructor Retorna a função construtora da string

// endsWith( pesquisar, comprimento? ) Retorna se uma string terminar com um valor especificado

// String.fromCharCode( n1, n2, ..., nX ) Retorna valores Unicode como caracteres

// includes( pesquisar, início? ) Retorna se uma string contém um valor especificado

// indexOf( pesquisar, início? ) Retorna o índice (posição) da primeira ocorrência de um valor em uma string

// lastIndexOf( pesquisar, início? ) Retorna o índice (posição) da última ocorrência de um valor em uma string

// length Retorna o comprimento de uma string

// localeCompare( compareString ) Compara duas strings na localidade atual

// match( match ) Pesquisa uma string por um valor ou uma expressão regular e retorna as correspondências

// prototype Permite adicionar propriedades e métodos a um objeto

// repeat( contador )	Retorna uma nova string com um número de cópias de uma string

// replace( pesquisar, novo valor ) Pesquisa uma string em busca de um padrão e retorna uma string onde a primeira correspondência é substituída

// replaceAll( pesquisar, novo valor )	Pesquisa uma string em busca de um padrão e retorna uma nova string onde todas as correspondências são substituídas

// search( pesquisar ) Pesquisa uma string em busca de um valor ou expressão regular e retorna o índice (posição) da correspondência

// slice( início, fim? ) Extrai uma parte de uma string e retorna uma nova string

// split( separador, limite? ) Divide uma string em uma matriz de substrings

// startsWith( pesquisar, início? )	Verifica se uma string começa com caracteres especificados

// substr( início, comprimento? ) Extrai um número de caracteres de uma string, de um índice inicial (posição)

// substring( início, fim? ) Extrai caracteres de uma string, entre dois índices especificados (posições)

// toLocaleLowerCase() Retorna uma string convertida em letras minúsculas, usando a localidade do host

// toLocaleUpperCase() Retorna uma string convertida em letras maiúsculas, usando a localidade do host

// toLowerCase() Retorna uma string convertida em letras minúsculas

// toString() Retorna uma string ou um objeto string como uma string

// toUpperCase() Retorna uma string convertida em letras maiúsculas

// trim() Retorna uma string com espaços em branco removidos

// trimEnd() Retorna uma string com espaços em branco removidos do final

// trimStart() Retorna uma string com espaços em branco removidos do início

// valueOf() Retorna o valor primitivo de uma string ou um objeto string
